// Package envclassify provides heuristic classification of secret keys
// into semantic categories: credential, endpoint, feature_flag, identifier,
// and unknown. Useful for reporting, auditing, and policy enforcement.
package envclassify
